# STEAM_DESKTOP — Responsive Meadow Amendment v1 — PM Activation Status

Дата: 2026-07-14
Обновлено: 2026-07-18
Статус: `PLAYER_PROFILE_PRE_FLUSH_CORRECTION_IMPLEMENTED / FOCUSED_FRESH_PROCESS_GATE_PASS / SUPERVISOR_RC0 / PROCESS_PASS / DIAGNOSTIC_PASS / FULL_CAPTURE_NOT_ACTIVATED / CAPTURE_HOLD / EVIDENCE_HOLD / UNSEALED / FINAL_RUNTIME_ART_USER_ACCEPTANCE_PENDING`
Владелец authority: User Owner / Producer / Project Manager
Art source owner: thread `019f5ad9-79c0-7b82-9964-3491f93bb7ff` — source handback complete; no active write ownership
Historical initial runtime integration writer: thread `019f5ce4-e63c-7d33-a586-d2d3031c8610`; ownership window released
Current writer: none; the completed D-029/D-024 remediation/correction writer was thread `019f6604-8ac6-7871-85c8-2c858a2240f3`, and any later full-capture writer/scope must be named by a new literal ACK
D-024 current authority: immutable Contract A `4f956a077d0a93575ef7b518fd0aa9fb409392a08fd4a48190364795bc9b5cbf`
Package:
`docs/drive/Shelter/03_DESIGN/04_DELIVERABLES/STEAM_DESKTOP__Art_Source_Responsive_Meadow_Left_Cluster_Amendment_v1/`

---

## 1. PM verdict

User-owner world/viewport authority is durably recorded as D-024. The four
Labrador/building/meadow visuals shown to the user are positively accepted as
the visual direction. This does not create runtime Art PASS, shipping approval
or authority for a broad pixel-regeneration loop.

The additive Art package completed its signed source-only wave and exact
QA/hash handback. Producer/PM accepts it in §11 only as source input for one
bounded runtime integration trial. Final runtime Art/user acceptance remains
open. Technical preflight is signed and §12 activates the new Codex brief as
`ACCEPTED / EXECUTABLE` for exactly one writer.

The initial mechanical and later authority-tool correction waves remain
historical in §12–§14. The subsequent D-029/D-024 observability, graceful-stop
and atomic-runner remediation is implemented and independently reviewed
`PASS` (`P0=0 / P1=0 / P2=0`). The later player-profile pre-flush correction
is implemented and its two-process focused ordinary-Terminal gate is `PASS`.
Capture is still not active: the 32-file canonical evidence root remains
`HOLD / UNSEALED`, and a full runner attempt requires a new user/coordinator
decision and literal full-capture ACK. Current factual state is recorded in
§16; §15 remains history.

## 2. Exact product rule

1. The PlayerBoot/player viewport continues to occupy 100% usable width.
2. Meadow must not be non-uniformly stretched.
3. Meadow is a seam-safe horizontally tileable Shelter pattern/module. It is
   repeated as needed to cover visible width and world width without a visible
   seam, black tail or transparent tail.
4. Game-field bounds and viewport bounds are independent.
5. When the gameplay field is narrower than the viewport at the current scale,
   the meadow continues to the viewport edge. The continuation outside the
   gameplay field is non-buildable and excluded from dog idle and selector-H
   activity.
6. When the viewport is narrower than the gameplay field, horizontal mouse-drag
   pan/scroll is required. Exact input ownership and transforms require the
   signed Game Design and Technical contracts below.
7. At every zoom, about 15% of viewport width at the right is preserved as empty
   meadow reserve: no buildings and no dog activity. It is composition/future-
   controls reserve only; no future controls are implemented by this decision.
8. Immediately after the rightmost buildable cell, the composition places one
   static decorative `Fence Boundary Marker`. Everything to its right is
   non-buildable tiled meadow continuation. The preferred marker faces inward
   through a separate authored positive-coordinate mirrored source/export;
   runtime negative scale is forbidden. The marker has no input, task,
   resource, output, reward, progression, collision or gameplay authority and
   is not a new gameplay entity/mechanic.
9. The existing left-cluster amendment goal remains bounded to the accepted
   current semantic roster and visual direction. Exact anchor placement must
   respect the right reserve and later signed contracts; the package name does
   not create new gameplay authority.
10. The supplied Crusaders Quest screenshot illustrates only the relationship
   between buildable boundary, continuing meadow and viewport edge. No CQ
   pixels, style, UI, buildings, characters, mechanics or density may be copied.
11. No new mechanic, gameplay entity, room or object transfer is authorized.

## 3. Superseded clauses

This record supersedes all earlier proposals that required:

- one unique authored meadow image for the full display width;
- prohibition of tile-copy/repetition for the meadow;
- a fixed 2992×224 player window;
- a centered 2992 lane with symmetric transparent gutters;
- a right-aligned 2992 lane with a left transparent gutter;
- any black or transparent side tail.

Only the meadow repetition rule is reversed: repeated buildings/anchors remain
forbidden. No non-uniform stretch or crop is allowed.

## 4. Signed owner contracts for Art resume

Exact authority readback:

```text
02_DECISIONS.md                                                e38cae98ff2db9a844149405f23492bcba962926524bb046bcf32ecc1c0c589d
01_CURRENT_STATUS.md                                           714f5cbdfe968f1002ce11d9244f2fe88ba3da26db467836f8ccbff466bbd6c1
STEAM_DESKTOP__CURRENT_CONTEXT.md                              c3971d2fdd583929f33ebe0a859028600a38b7523a35710993db491bfd868137
STEAM_DESKTOP__First_48_Hours_Playable_Roadmap_v1.md           522440398e8117d2434920e04c1d84642e8893bd73c94f6a8e12ae033d650463
this PM activation/status record                               45eb85e0fbc100d0e7f9b85efad1bc418f37e5a434cb685bf1002054da46f9ef
STEAM_DESKTOP__Gameplay_Field_And_Viewport_Semantic_Contract_v1.md e103d836427e3bb5054a183149dd97b095e91b5a4195c752ac5f4da19a00854c
GAME_DESIGN__CURRENT_CONTEXT.md                                d3c393df311e85bdd8f133c40f5be189bafc64db17de3f6dcd1b6230c096d563
```

Signer results:

- Game Design: accepted semantic contract plus
  `SIGNED_GD_LEFT_SENTINEL_INTERPRETATION`;
- Technical/Codex: `SIGNED_TECHNICAL_READBACK`;
- Producer/PM: exact-file convergence PASS and explicit source-only resume in §9.

### Game Design readback

Game Design signed an exact contract for:

- gameplay-field/buildable boundary versus viewport;
- the rightmost buildable cell and the marker immediately after it as the
  readable boundary, without collision or gameplay authority;
- non-buildable meadow outside gameplay bounds;
- dog idle and selector-H exclusion zones;
- the right-side approximately-15% reserve at every zoom;
- mouse-drag pan as navigation/presentation with zero progression output and no
  change to the accepted `3 + 2` irreversible player-gate budget;
- fail-closed behavior during authoritative task/trip/delivery and restore/error
  states.

### Technical readback

Technical/Codex signed an exact contract for:

- seam-safe module import/render repetition with no stretch, crop or tails;
- independent world/game-field/viewport coordinate spaces;
- zoom-dependent tile coverage and right-reserve calculation;
- pan bounds and screen/world/input/mouse-passthrough transforms;
- station, dog/H and interaction exclusion enforcement;
- one positive-scale marker render from the dedicated authored mirrored export,
  with no runtime negative-scale shortcut and no collision/input authority;
- checkpoint/save/schema impact, expected file topology and regressions. The
  former universal desktop-evidence requirement is superseded by D-025 and §13.

### Completed Art source deliverable after explicit PM resume

The retained Art owner provided and PM read back:

- a seam-safe horizontally tileable Shelter meadow module with visible seam QA;
- the accepted current Labrador/building direction without a broad regeneration;
- a separate authored positive-coordinate mirrored `Fence Boundary Marker`
  source/export, with provenance and hashes;
- responsive composition evidence proving the marker immediately after the
  rightmost buildable cell, no buildings/dog activity to its right and about
  15% empty meadow reserve at every declared zoom;
- no CQ-derived pixels and no runtime/code mutation.

## 5. Activation sequence

```text
D-024 authority record — DONE
→ Game Design exact-file readback — SIGNED
→ Technical exact-file readback — SIGNED
→ separate Producer/PM exact-file review and explicit ART_RESUME — AUTHORIZED
→ retained Art owner resumes this package source-only — DONE
→ Art package QA/hash handback — SOURCE_AMENDMENT_READY
→ PM/User source acceptance — ACCEPTED_FOR_BOUNDED_RUNTIME_TRIAL
→ separate PREPARED Codex brief — DONE
→ Technical exact-file preflight — SIGNED
→ PM executable activation — ACCEPTED / EXECUTABLE
→ one Codex integration writer `019f5ce4-e63c-7d33-a586-d2d3031c8610` — MECHANICAL HANDOFF DONE / HISTORICAL WINDOW RELEASED
→ Technical macOS/capture reconciliation — SIGNED
→ immutable Contract A amendment + bounded validator/runner correction — PASS
→ PM Phase 2 factual sync / final brief SHA C — DONE
→ D-029/D-024 observability + atomic-runner remediation — IMPLEMENTED
→ independent no-Godot re-review — PASS / P0=0 / P1=0 / P2=0
→ PM factual sync / whole brief SHA K — DONE
→ player-profile pre-flush correction — IMPLEMENTED
→ focused fresh-process ordinary-Terminal gate — PASS / rc0 / PROCESS PASS / DIAGNOSTIC PASS
→ PM §0J factual sync / whole brief SHA M — DONE
→ new user/coordinator decision + literal full-capture ACK naming A/M/final pins/pinned evidence/writer/scope/failed-stage disposition — REQUIRED / EVIDENCE HOLD
→ full Steam Godot 4.7.1 runtime/control/capture attempt — ONLY AFTER NEW ACK
→ immutable seal + independent Art/user runtime review — AFTER CAPTURE
```

Game Design and Technical signatures did not automatically resume Art. The
separate Producer/PM activation required after both handbacks is recorded in
§9. That package-local source wave is now complete; §11 records the later
bounded source acceptance. No Art write ownership or runtime authority remains
active under the old resume.

## 6. Final package readback

Final verdict: `SOURCE_AMENDMENT_READY / SOURCE_ONLY /
NOT_RUNTIME_EXECUTABLE`.

```text
README.md                 e92e5ab61850b380e7db0317e7db469ae0ae9515eb16f208abfb85940c058487
PROVENANCE.md             5ccbfcac2bfa1c185a46721058ffe4e6961a84e7969fc932f98efec3d21a63f5
REFERENCE_READBACK.md     e60aba402c7c9b4ba2291ea85ec96bf50f6117a36a0e47ff202028bd460724db
ART_QA.md                 f8e2b983cbd8c8494e917c15aba97810c7659a85a0ed60160b66ad5fecf64e78
SOURCE_MANIFEST.json      66dba9bb18a7932fc079055b2bce7645484f3bc084ea3c1427169b2efa81bd89
QA_REPORT.json            82aa32d635b96f82cf52e101485d91cb374e24859d386696d4728b612b44ea2c
INVENTORY.txt             76da63d92b3011082a0c07ff424e3ad4d00465f899c9f735429bdb9b2bc6e293
HASHES.sha256             220a9532f54b8f8ae813f32ac02ef28e35a5d2bde6ded318ecb08a43319e43bf
tools/build_amendment.py  4cef60122de12cf2ae6de83ffc4fb4ed99d77ee24d6fa242aa92b6558a1de2f2
```

Readback: `51` files, ledger `50/50 PASS`, inventory `51/51`, QA
`48/48 PASS`, deterministic rebuild byte-identical.

Accepted inputs:

- meadow ORA `fbd9e9a03a54836933bf912ade049a97d0eaf79c26272fe919c0e626ce8093ea`;
- meadow RGBA `1cb5845141ad80beba303bc6e0805f10954310eb783a9dfb5ac5f1354e144d40`;
- marker ORA `e04a73af66742472c293eb3bd5925c22d10933243f46f27500713839c08c4111`;
- marker RGBA `e0237a29119a318cb7b5acb431ed17e7b70d7da3d5386883b335d16ba7416036`;
- responsive measurements
  `587072561283c0f85aef3fed0af8e37a2be8317b86d3ba47c09325451ffc0f0a`.

CQ remains layout-only with zero pixel/style reuse. The `3840` source-board
vertical clipping observation remains a runtime Technical/Art capture gate.

## 7. Stop conditions

Stop and return to Producer/PM if any of the following occurs:

- Art exceeds the exact source-only resume recorded in §9;
- a seam-free tile cannot be produced honestly from permitted Shelter sources;
- the solution stretches/crops the meadow or creates a seam/black/transparent tail;
- buildings or dog activity enter the right reserve or meadow outside gameplay bounds;
- the marker is missing, placed anywhere except immediately after the rightmost
  buildable cell, duplicated, interactive/collidable or runtime-mirrored;
- viewport, game field and buildable bounds are collapsed into one authority;
- pan/zoom changes gameplay, persistence, progression or the `3 + 2` gate budget;
- the CQ screenshot would need to be copied or traced;
- a new mechanic/entity/room/transfer, broad pixel loop or frozen-package/runtime
  mutation is required;
- exact source rights, provenance, hashes or responsive QA cannot be stated.

## 8. Non-effect

This record does not edit or accept the Art package, does not promote files to
`approved_art_files`, does not change the accepted Labrador A–H rows, and does
not authorize runtime/code/assets/import/evidence work. R48-05B, rooms,
onboarding and background/minimize/performance remain later.

## 9. Explicit PM ART_RESUME — 2026-07-14

Historical source-stage verdict, later superseded for execution by §12:

```text
ART_RESUME_AUTHORIZED
SOURCE_ONLY
NOT_RUNTIME_EXECUTABLE
```

Producer/PM authorizes the retained Art owner thread
`019f5ad9-79c0-7b82-9964-3491f93bb7ff` to resume writes only inside:

```text
docs/drive/Shelter/03_DESIGN/04_DELIVERABLES/STEAM_DESKTOP__Art_Source_Responsive_Meadow_Left_Cluster_Amendment_v1/
```

This is not source acceptance and not permission to edit frozen source,
approved files, runtime/code/assets/import/evidence, PM/GD/Technical documents
or any Codex brief.

### 9.1 Fixed Technical envelope

- `WORLD_WIDTH` and gameplay field remain `[0.0, 1740.0]`.
- No PlayerBoot/window/platform/checkpoint/profile/game-system mutation.
- With viewport width `V`, `p = 0.15` and positive zoom `z`:

```text
z_min(V) = 0.85 × V / 1740
z_default = z_min(V)
camera_default_x = 0
allowed z >= z_min(V)
camera_max = max(0, 1740 - 0.85 × (V / z))
```

- At default and right clamp, field boundary `x = 1740` maps to `0.85V`; the
  accepted right reserve is `13–17%` of viewport width.
- One accepted meadow tile is loaded once, world-anchored and repeated only for
  the visible range with uniform scale. Buildings, static layers and Bicycle
  never repeat.
- One marker is drawn from a dedicated authored-positive mirrored source. Its
  boundary plane is `x = 1740`; its nontransparent body is fully exterior at
  `x >= 1740`; x/y scale stays positive. It has no node/entity/collision/input
  authority and no runtime mirror.
- Draw order remains:

```text
meadow tile
→ static 00–09
→ Bicycle
→ static 11–15
→ actors
→ layer16
→ Fence Boundary Marker
→ current resources/cues
```

- A future mouse-drag candidate begins only on non-interactive in-field ground,
  after Control/player-gate precedence, with an `8 screen px` threshold and only
  when `camera_max > 0`. Exterior/marker space at `x > 1740` stays click-through.
  The existing full-width passthrough polygon and `Show UI`/right-band control
  are later Codex-cleanup issues; no UI may cross the exterior reserve.
- All current A–H anchors/roots remain in field. Any invalid/exterior derived
  root fails closed. Pan/zoom creates zero gameplay/H output and is non-persisted.
- `offscreen_left = -160` is explicitly accepted by Game Design only as the
  pre-existing hidden D-013 external-TripTask absence sentinel for
  Dachshund/Bicycle. It is not meadow/world/buildable/station/A–H/idle space and
  does not weaken the `[0,1740]` fail-closed rule for visible roots.
- PlayerBoot/platform change is unnecessary. A future native passthrough failure
  returns `STOP_D024_PLATFORM_PASSTHROUGH`; it does not expand platform scope.

### 9.2 Superseded implementation assumptions

D-024 supersedes the older implementation assumptions of:

- a unique authored tail/full-width corridor;
- a fixed `2992` player window;
- universal `1 screen px / source px` mapping;
- a final immutable `41-file` count after the additive source amendment;
- `no crop` where that phrase incorrectly forbids normal viewport clipping.

Retained without change:

- `1740` gameplay field and existing `1740/2992` mapping for current one-shot assets;
- current 41-file corpus as existing input, not final additive count;
- semantic order, A–H authority, no-transfer boundary and one parent render lane.

## 10. Satisfied Art source-only acceptance input

Art returned all of the following; the exact package is now frozen.

### Meadow

- editable master and lossless export paths, exact hashes, provenance,
  right-to-use declaration and explicit zero CQ derivation;
- integer PNG dimensions, colour mode and alpha contract;
- declared seam identity region plus machine-readable RGBA and alpha seam proof;
- exact `source_px_per_runtime_unit` or `tile_world_width`;
- baseline, vertical bounds, world phase/origin;
- repeat/filter/mipmap/import recommendation and seam QA at declared responsive scales;
- no alpha, black or transparent holes/tails.

### Fence Boundary Marker

- separate authored positive-coordinate mirrored master/export;
- hashes/provenance, PNG dimensions and alpha bbox;
- local boundary plane, pivot, baseline/contact y and
  `source_px_per_runtime_unit`;
- proof that the nontransparent body stays fully exterior after positive transform;
- placement metadata at world `x = 1740` and the signed draw slot;
- no runtime mirror and no collision/input/entity metadata.

### Responsive Art QA

- default and right-end boards for agreed widths/scales;
- seam, black-background and checker evidence;
- measured `13–17%` right reserve;
- no repeated building cluster, dog activity or unauthorized exterior content;
- preserved accepted Labrador/building/meadow direction;
- no broad pixel loop or image regeneration unless Art first proves bounded
  deterministic source work insufficient and returns to Producer/PM;
- CQ remains layout-only with zero pixel/style reuse.

The independently accepted four direction-only promotion hashes remain:

```text

Approved Library 99628aefef58f5e028d160df110741e4a801d3f323831cb08618e4599d63a2c5
Promotion Record 6f6a499f2cf2c2b3dfa2b8967adc973a9a8c6c0b4be31b4077d6cb4938ddde40
inventory 17
```

They remain flattened opaque RGB visual direction, not runtime or master input.

## 11. PM/User source acceptance — bounded runtime trial

Verdict:

```text
ACCEPTED_SOURCE_INPUT
BOUNDED_RUNTIME_TRIAL_ONLY
RUNTIME_NOT_YET_EXECUTABLE
```

The durable acceptance record is:

```text
docs/drive/Shelter/03_DESIGN/04_DELIVERABLES/STEAM_DESKTOP__Art_Source_Responsive_Meadow_Left_Cluster_Amendment_v1__PM_User_Source_Acceptance.md
```

PM independently rehashed the package, verified ledger coverage, read the
responsive/seam/marker evidence and sampled original alpha pixels. The dark
region seen in one resized `right_end` preview was diagnostic-preview behavior,
not an opaque source tail: checker evidence covers the full width and every X
column in the lower meadow band has occupied alpha for all six declared
width/mode cells.

The separate implementation brief is prepared at:

```text
docs/drive/Shelter/04_DEVELOPMENT/STEAM_DESKTOP__Codex_Brief__D024_Responsive_Meadow_Marker_And_Player_Presentation_Cleanup_v1.md
```

Technical thread `019f57a6-da0c-7e00-a40e-a2e768247436` returned
`SIGNED_TECHNICAL_PREFLIGHT` against prepared brief SHA
`d184601d3844163eecc63bb3a9cb33ae9a61e260e704ac2f371ada7ffb8d22db`.

## 12. Explicit PM runtime activation — 2026-07-14

Verdict:

```text
ACCEPTED
EXECUTABLE
ONE_WRITER_ONLY
FINAL_RUNTIME_ART_USER_ACCEPTANCE_PENDING
```

The activated brief is:

```text
docs/drive/Shelter/04_DEVELOPMENT/STEAM_DESKTOP__Codex_Brief__D024_Responsive_Meadow_Marker_And_Player_Presentation_Cleanup_v1.md
```

Single writer: thread `019f5ce4-e63c-7d33-a586-d2d3031c8610`.

Accepted Technical topology:

- add two byte-identical PNGs plus two new `.import` sidecars and one
  presentation-only JSON; existing `41 PNG + 41 .import` remain untouched,
  final corpus `43 + 43`;
- modify only `vertical_slice_demo.gd` as runtime owner; no scene mutation;
- add dedicated D-024 test runner, test, validator, capture entry and new
  immutable evidence root;
- use the exact tiled-meadow, positive marker, pan/input/passthrough and A–H
  boundaries in the brief;
- apply the signed `3840` vertical-fit cap; any native material clipping is
  `STOP_D024_VERTICAL_FIT_3840`;
- hide the prototype visibility button in ordinary mode and retain existing
  `KEY_H` as two-way Hide/Show; native recovery failure is
  `STOP_CUE_PRESENTATION_UNRESOLVED`;
- keep PlayerBoot/platform/project, profile/checkpoint/schema/33 cursors, game
  systems/connectors, Labrador adapter/bindings, existing corpus, source,
  approved files and prior evidence no-touch.

The former Windows warning clause is superseded by D-025. Windows is not in the
current implementation, QA, evidence, WARN, blocker or acceptance queue.
Runtime Art PASS and final user acceptance remain later gates after immutable
macOS self-capture and independent review.

## 13. Historical D-025 platform/capture sync and initial capture-only RESUME — 2026-07-14

Current platform sequence:

```text
MACOS_ONLY through the full development/assembly/QA/acceptance phase
WINDOWS_PRE_RELEASE_WAVE_NOT_ACTIVATED
WINDOWS_ABSENCE_IS_NOT_WARN_OR_BLOCKER
```

Windows remains a future target, but only a separate explicit Producer/PM
pre-release activation may open compatibility/port/smoke/certification work.
D-024 may close its current gate on macOS-only evidence.

Visual evidence has exactly two allowed paths:

1. Existing Godot self-capture — viewport `save_png`, State Connector
   `capture.screenshot`, 10-second / 2 FPS `capture.video.start` PNG sequence,
   and the dedicated D-024 SubViewport test runner.
2. macOS Screenshot UI through Computer Use only for desktop/native-window or
   other system context outside the Godot viewport.

There is no third/ad-hoc path. Headless/dummy output without a real framebuffer
cannot close visual proof. If both paths are unavailable, the evidence remains
`BLOCKED / UNSEALED`.

Mechanical handback accepted:

```text
MECHANICAL_PASS_CANDIDATE
exact runtime corpus 43 PNG + 43 .import
D-024 validator/tests/A–H/33 cursors/profile/restart/SIGKILL/Retry PASS
evidence root 31 files / no HASHES.sha256 / seal count 0
unsealed ledger 3c4a58cdebfa1244cd6e432627c10e3e085568996f83894bacee18c1f0ad7828
```

The observed AppKit/HIServices abort happens before Godot/project initialization
inside the current Codex host. It is an environment limitation, not a D-024
runtime/product failure; the same direct GUI launch must not be repeated.

Technical reconciliation:

```text
SIGNED_TECHNICAL_RECONCILIATION / MACOS_ONLY / GODOT_SELF_CAPTURE_READY
```

The D-024 test runner already self-captures the complete 27-PNG visual matrix,
and the State Connector already supplies still/video capture. No new capture
subsystem is needed or authorized. External desktop imagery is not required for
current acceptance; native macOS window/input/passthrough remains a separate
mechanical probe where testable.

Historical Producer/PM capture-only verdict:

```text
CAPTURE_ONLY_RESUME_ACCEPTED / EXECUTABLE / ONE_WRITER_ONLY / MACOS_ONLY
```

Historical sole writer for that activation: thread
`019f5ce4-e63c-7d33-a586-d2d3031c8610`. That ownership window is released.

Exact exclusive mutation scope:

```text
steam/tests/vertical_slice_visual/test_labrador_visual_binding.gd
steam/tools/capture-d024-responsive-presentation.sh
docs/drive/Shelter/03_DESIGN/04_DELIVERABLES/STEAM_R48_05A_D024_RESPONSIVE_PRESENTATION_RUNTIME_CAPTURE_v1/**
```

The writer must first remove/skip only the Labrador runner's external desktop
capture call/expectation that reaches `/usr/sbin/screencapture`, then remove the
desktop expectation and all Windows fields/WARN from the D-024 capture
manifest/seal. The existing self-capture and macOS mechanical/native checks stay.
Only after those edits may the full capture runner be used in a normal
GUI-capable macOS Godot session and the existing evidence root regenerated and
sealed.

No-touch: `vertical_slice_demo.gd`, connectors, PlayerBoot/platform/project,
runtime gameplay, assets/imports, profile/checkpoint/A–H, source/approved
packages and previous sealed evidence. No new capture subsystem, no stage,
commit or push. Final runtime Art/user acceptance remains pending after the
sealed macOS evidence handback.

## 14. D-024 authority-contract correction PASS / capture reactivation pending — 2026-07-15

Current factual verdict:

```text
TOOL_CORRECTION_PASS
CAPTURE_REACTIVATION_PENDING_COORDINATOR_ACK
EVIDENCE_HOLD
UNSEALED
FINAL_RUNTIME_ART_USER_ACCEPTANCE_PENDING
```

Current authority and result ledger:

```text
authority_contract_scheme=raw-bytes-between-markers-v1
authority_contract_sha256_A=4f956a077d0a93575ef7b518fd0aa9fb409392a08fd4a48190364795bc9b5cbf
pre_code_brief_sha256_B=eb32b3d791e32d0abbcb1a790cac9acc8588058ac452f4b1dee810be22f43d7b
final_phase2_brief_sha256_C=86c5a7a99f3d9ca4ef5874eb1ebde958749564398993a5572534da4323a2876a
validator_sha256_V=a0b4c0da2679118083409f306a77db4be6952e8d268fc3bcff38f90975c09f0f
runner_sha256_R=add7777692d0f1eefbf0e2e4ce5b8984722b1433fb735835e58ad0137ab5d949
labrador_no_touch_sha256=14a37141f418cc38e998d47a2ae678b5ade1c5ebf1a3d3022b4ffd577d1744b8
evidence_root_files=31
evidence_seal=absent
unsealed_ledger=30/30
```

Validator `--authority-only` prints exact A; normal validator passes; strict
marker and temporary-copy mutation matrix passes `6/6`; Python compilation,
runner syntax and diff checks pass. The current validator and runner are V/R
above. Historical hashes `721191…`, `00c89…`, `d94969…` and mutable PM-status
pin `98e3…` remain workflow/evidence provenance only; they are not current
compiled authority gates.

The bounded tool-correction ownership window is closed. Capture remains
inactive while evidence is on HOLD. The current sole writer reserved for the
later capture wave is thread `019f6604-8ac6-7871-85c8-2c858a2240f3`, but that
writer may not launch Godot, the runner or write evidence until the coordinator
issues a literal capture-reactivation ACK naming exact A and C.

After that ACK, the only current next action is one launch of the existing
runner from a visible GUI-capable macOS terminal, followed by immutable seal and
independent Art/user review. No new runner, capture path, gameplay, runtime,
asset or product scope is authorized.

## 15. D-029/D-024 no-Godot remediation review PASS / bounded real-run ACK required — 2026-07-16

Current factual verdict:

```text
D029_D024_OBSERVABILITY_ATOMIC_RUNNER_REMEDIATION_IMPLEMENTED
INDEPENDENT_NO_GODOT_REVIEW_PASS
P0=0 / P1=0 / P2=0
CAPTURE_BLOCKED
EVIDENCE_HOLD
UNSEALED
BOUNDED_REAL_RUN_REQUIRES_NEW_USER_COORDINATOR_DECISION_AND_LITERAL_ACK
FINAL_RUNTIME_ART_USER_ACCEPTANCE_PENDING
```

Current authority and evidence boundary:

```text
authority_contract_sha256_A=4f956a077d0a93575ef7b518fd0aa9fb409392a08fd4a48190364795bc9b5cbf
current_whole_brief_sha256_K=ccb81f8a7f881ad078dad54bcd811dad2616aa36f843b2bee0ca67c2487d26ed
evidence_root_files=32
evidence_seal=absent
evidence_tree_digest=4ca49b1d9cd0616d434eb534464087c75cebcd4972122356ad9197ec59cdd378
transient_stage_or_rollback_siblings=0
staged_paths=0
```

The exact eight-file D-029/D-024 implementation and focused no-Godot matrix
were independently reproduced: authority-only and normal validator PASS,
observer suite `22/22 PASS`, shell syntax PASS and exact scope/pin,
expected-persistence seam, `17 -> 18`, no-hard-kill and no-CA-allowlist checks
PASS. The final hashes are recorded in active brief §0H.

This PM sync activates nothing. Real Steam Godot `4.7.1`, Continue runtime
scenarios, actual PlayerBoot control ACK, GUI/native capture, manifest, seal,
promotion and final Art/user review remain not run. The CA diagnostic remains
`REAL / UNRESOLVED / NOT ALLOWLISTED`; any recurrence is diagnostic FAIL,
supervisor rc `73` and `STOP` before capture/seal.

The sole next gate is a new user/coordinator decision and literal bounded
real-run ACK naming unchanged A, current brief K, the final eight hashes, the
pinned 32-file evidence tree and exact writer/scope. No automatic launch follows
this documentation sync.

## 16. Player-profile focused fresh-process gate PASS / full capture ACK required — 2026-07-18

Current factual verdict:

```text
PLAYER_PROFILE_PRE_FLUSH_CORRECTION_IMPLEMENTED
FOCUSED_FRESH_PROCESS_GATE_PASS
SUPERVISOR_RC0
PROCESS_PASS
DIAGNOSTIC_PASS
FULL_CAPTURE_NOT_ACTIVATED
CAPTURE_HOLD
EVIDENCE_HOLD
UNSEALED
FINAL_RUNTIME_ART_USER_ACCEPTANCE_PENDING
```

Current authority and implementation:

```text
authority_contract_sha256_A=4f956a077d0a93575ef7b518fd0aa9fb409392a08fd4a48190364795bc9b5cbf
current_whole_brief_sha256_M=f2d26ebde2f27dde1c75cd82304d30f850b015834ed780947fbb6972bb111130
current_brief_section_0J_sha256=9335f62b600578d8d935fd3e3a76722434c54efc6cf0d66df4909cdb9f10fcf3
test_player_profile_store_sha256=cbc2eddf7e4048a9beb071e3b6b525baecfbcd71bdd3417ec617e7604b01dc65
capture_runner_sha256=94cfed6865ecdbd7b6b1a93ebc5228cbfb7e8702406cb3071408514dc0e4aa9d
```

The exact fresh `snapshot-create after_temp_write` result is
`8442e0a2419b6fb072eb6078017d817c69715b5298e0ee2691e84c7769bcdb1e`.
The separately spawned `snapshot-inspect expected no_valid_profile` result is
`4784d87ace632d3cb9a767f67004efcc663e816b83165875a5ec2b3d9b162662`.
Both used exact Steam Godot `4.7.1.stable.steam.a13da4feb` and returned
supervisor rc `0`, process `PASS`, diagnostic `PASS`, empty matches, exit `0`,
finalized raw logs and dead child PID. Focused diagnostic root:

```text
files=18
png=0
seal=absent
tree=39ea48e8a185459b6475d1574847710dd0a44caacaf430df62fe02b350e9b647
```

Canonical evidence remains exactly 32 files, PNG `0`, no seal and tree
`4ca49b1d9cd0616d434eb534464087c75cebcd4972122356ad9197ec59cdd378`.
The retained failed stage remains exactly 144 files, PNG `0`, no seal and tree
`00116ac0280ffc72c5f3db3961857c8f3ed815447cc65095d2dc29d5a80badcf`.
Neither diagnostic root is promotable evidence.

CA/certificate, `editor_data`, AppKit and HIServices are historical observations
and are not current blockers: they were absent from the 13/14 ordinary-Terminal
run and this focused gate. They remain not allowlisted; any recurrence is
diagnostic FAIL, supervisor rc `73` and `STOP` before capture/seal.

This PM sync activates nothing. The full runner, Continue/full runtime matrix,
actual PlayerBoot control ACK, native/GUI self-capture, 27 PNG, manifest, seal,
promotion and final Art/user review remain not run. Only a new literal
full-capture ACK naming A/M, final governed pins, the canonical tree, exact
writer/scope and authorized failed-stage disposition may activate one attempt.
There is no automatic retry.
