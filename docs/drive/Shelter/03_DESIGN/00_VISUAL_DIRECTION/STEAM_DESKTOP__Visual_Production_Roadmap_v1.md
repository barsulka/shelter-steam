# STEAM_DESKTOP — Visual Production Roadmap v1

Дата: 2026-07-22
Статус: active art roadmap
Владелец: Art Director
Назначение: провести Visual Shell Lock без скрытого изменения mechanics или asset roster.

---

## 0. Current visual task

Static exploration is complete: selected H GRID32 is `USER_ACCEPTED / PASS`.
Current visual task is faithful live integration and user review at every
`min/default/max × 50/100/150/200` state. CQ Hero Town remains the harmony bar,
not an asset source or gameplay spec.

Accepted static key: canvas `2992×480`; grid `y[441,473)`, height `32`, earth
margins `25/7`; rear/middle/front exclusive alpha-bottoms `367/386/402`; exact
source hashes and transform math live in the active independently verified
selected-H Codex brief.

## 1. Locked inputs

- exact selected H current meadow roster plus Labrador;
- exact accepted Shelter sources and hashes from the active Codex brief;
- selected H as static visual authority; D-030 only as mechanical baseline;
- min/default/max windows;
- zoom `50/100/150/200`;
- target runtime must contain no legacy fences, polygon dogs or artifacts.

Composition, scale, placement, depth and camera are no longer adjustable inside
the current wave. Any change requires explicit user re-open/re-approval. Art
Director may not add mechanics, buildings, dogs, room behavior, new asset
taxonomy or production output.

## 2. Re-open-only batch policy

No new batch is active. If the user explicitly reopens the static art contract,
return to 3–5 materially different variants using the same accepted roster and
stop for a new direct selection; micro-offset/palette variants remain invalid.

## 3. Review sequence

```text
selected H GRID32 static USER_ACCEPTED / PASS
→ separate Codex brief and live scene
→ min/default/max × four zoom manual review
→ explicit user Visual Shell Lock
```

No validator, comparison score or Art self-review can grant the final lock.

## 4. After lock

Any visible modification—including scale, placement, camera, roster, asset,
ground, underground, card position or room presentation—requires renewed user
approval. Technical fixes that change pixels are visible modifications.

## 5. Later Art gates

Only after live Visual Shell Lock:

1. entity micro-card/badge visual review;
2. move ghost/foundation/boxes/smoke visual review;
3. one selected house room panel review;
4. integrated shell review.

Each stage uses the manual cadence in the canonical shell roadmap and stops for
user review. Do not pre-produce later visuals in the first composition task.

## 6. Stop conditions

- accepted assets cannot support a coherent composition;
- a variant requires a new mechanic/asset/roster item;
- static composition hides known runtime problems instead of solving them;
- legacy visuals are retained in the proposed runtime target;
- readable result depends on a window/zoom outside the required matrix;
- work drifts into cards, move or room before the live Visual Shell Lock.

## 7. Active implementation route

```text
docs/drive/Shelter/04_DEVELOPMENT/STEAM_DESKTOP__Codex_Brief__Selected_H_Visual_Shell_Runtime_Integration_And_Live_Matrix_v1.md
```
