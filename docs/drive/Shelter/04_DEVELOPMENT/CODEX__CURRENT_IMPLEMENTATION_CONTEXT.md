# CODEX — Current Implementation Context

Обновлено: 2026-07-23
Статус: active current-summary
Владелец: Codex / Project Manager
Назначение: текущий dev/Codex вход без completed briefs и chronological logs.

---

## Current implementation truth

- Steam/Desktop ordinary player route, persistence/checkpoints and implemented
  First Day/Day 2 causality exist as regression baseline.
- D-030 is the current runtime baseline: fixed 26-cell meadow period, no
  stretch, whole-game zoom `50/100/150/200`, dynamic strip height and
  alpha-aware pointer surface.
- D-024 ordinary-Terminal evidence is sealed and mechanically valid for the
  pre-D-030 runtime. Its fixed runner/test/normal validator/capture workflow is
  immutable evidence, not a current D-030/Selected-H applicability gate.
- Selected H GRID32 static composition has explicit user
  `USER_ACCEPTED / PASS` from 2026-07-22. Live runtime is not yet locked.
- The bounded runtime/live-matrix brief remains active; its Checkpoint 1 is
  USER_ACCEPTED and Checkpoint 2 is HOLD. The independently docs/brief-PASSed
  D-032 current regression-profile brief is now active as its prerequisite.
  Its D-033 macOS height amendment received independent docs/brief PASS at
  freeze `79cea50502aff44e0dd17265cdb85fb2347ec1d9c5cba855edb714e8622b1fe6`;
  the re-authorized run completed `12/12` states and `24/24` captures but its
  strict validator remained real `FAIL`/ineligible on the default × 50% pointer
  model. User-approved D-034 now makes projected canonical canvas the exact
  Selected-H render/pointer domain and true exterior transparent/click-through.
  Both active-brief amendments independently passed docs verification at freeze
  `9cd3ac289c90e2c3a4f8dd605b82229118e9f9743ca9c346c5e8cd379dca5e8b`.
  Coordinator/Root/King thread `019f8bac-0007-7962-ad8a-ca31fb95ac7f` read the
  accepted contract and recorded `ROOT-APPROVED` bounded continuation. Its
  status/provenance record passed independent read-only verification. The same
  author was dispatched but stopped `BLOCKED BEFORE SPAWN`: unchanged observer
  requires system-temp `HOME`/output while the prior recovery required
  repo-local temp. User-approved Route A permits transient system temp only with
  complete inventory/hash/byte-verified promotion into unique repo-local
  `tmp/`. A later least-privilege config/profile experiment received independent
  `PASS CONFIG ONLY`, while named-profile GUI runs reproduced pre-project
  AppKit/HIServices `SIGABRT`; both are real historical evidence, not current
  D-034 code verdicts. Direct user authority now isolates permission/private-tmp
  diagnosis in a separate non-blocking background task and resumes current
  production «по-старинке» under effective `:danger-full-access` / Full Access,
  with unchanged `/private/tmp` runner behavior and no named-profile/Seatbelt/
  network-disabled/config/UI prerequisite. Full Access route-reset docs received
  independent `PASS DOCS ONLY`. The first governed production run then stopped
  real `rc74`/ineligible at `19/24` captures under the spawn-anchored `6.0s`
  readiness maximum. A bounded `_d034_image_profile_metrics` performance
  bugfix plus differential fixtures preserved exact metrics/common PNG bytes
  and advanced its one run to `23/24`, but that run also stopped `rc74` before
  the final CLEAN capture, matrix and readiness token. Both remain historical
  FAIL/ineligible, not PASS.
- Direct user approval now sets exact `15.0s` only for the governed D-030
  Selected-H current-presentation route/profile. It remains a spawn-anchored
  maximum and returns immediately on successful readiness. Implementation and
  runtime remain `HOLD` pending this docs amendment's independent `PASS`,
  smallest route-only wiring and fresh static gates.
  Full live Visual Shell Lock remains `OPEN / NOT GRANTED`, and Interactive
  Shelter Shell is not authorized by either brief.

## Current priority and route

```text
selected H static PASS → runtime checkpoint 1 USER_ACCEPTED
→ D-033 amendment independent PASS → coordinator re-authorization
→ retained 12-state run / strict validator FAIL → accepted D-034
→ independent docs/brief PASS → Root/King bounded re-authorization recorded
→ independent read-only PASS → author BLOCKED BEFORE SPAWN on temp boundary
→ user-approved Route A evidence promotion
→ config/profile PASS → named-profile GUI SIGABRT reproduced
→ direct user reset: permission diagnosis moves to non-blocking background
→ effective Full Access production route + unchanged system temp
→ route-reset docs PASS → production rc74 at 19/24 under 6.0s
→ performance bugfix static PASS → production rc74 at 23/24 under 6.0s
→ direct user approval: exact D-030-only readiness maximum 15.0s
→ independent docs verification → narrow route-only budget wiring
→ static/protected/differential PASS → one governed production run
→ fresh governed 12-state run → verified repo-local evidence promotion
→ relevant suites → independently pass current profile
→ resume checkpoint 2 → paired live matrix → user four-zoom acceptance
→ separate briefs for cards → move → rooms → integrated shell
```

Codex must not start from the old First 48 Hours/Day 1/Day 2 roadmap. The
canonical current contract is:

```text
docs/drive/Shelter/02_PRODUCTS/01_STEAM_DESKTOP/STEAM_DESKTOP__Game_Design_Roadmap_v2.md
```

## D-024 immutable exception

Do not edit or delete:

```text
STEAM_DESKTOP__Codex_Brief__D024_Responsive_Meadow_Marker_And_Player_Presentation_Cleanup_v1.md
../03_DESIGN/04_DELIVERABLES/STEAM_R48_05A_D024_RESPONSIVE_PRESENTATION_RUNTIME_CAPTURE_v1/
```

Exact authority:

```text
Contract A: 4f956a077d0a93575ef7b518fd0aa9fb409392a08fd4a48190364795bc9b5cbf
sealed whole-brief SHA: cc6d7fa778b85eebd6d6307dba33efa52518aa62911287dd15ee0d9c7dd5c669
evidence ledger: 2066 entries, verified 2026-07-21
```

Also preserve all Markdown pinned by current Labrador/D-024 validators and
source-package ledgers. These files are retained for exact consumers, not as
current product direction.

## Required dev read route

```text
PROJECTS_RULES.md
AGENTS.md
steam/AGENTS.md
steam/README.md
docs/repo/adr/README.md
docs/repo/status/CODEX_CURRENT_STATUS.md
this file
STEAM_DESKTOP__Codex_Brief__Selected_H_Visual_Shell_Runtime_Integration_And_Live_Matrix_v1.md
STEAM_DESKTOP__Codex_Brief__D030_Selected_H_Current_Presentation_Regression_Profile_v1.md
```

This brief requires Accepted ADR-0001 and ADR-0002; every Godot process/capture
path also requires ADR-0004. Save/move recovery remains future ADR-0003 work.

## Interactive Shell implementation constraints

Future briefs must preserve:

- one live Godot runtime authority;
- one move at a time, source+destination reservation and no post-confirm cancel;
- stable save/reload mid-move;
- three render/occupancy layers only;
- no visible gameplay/economy output;
- locked visual composition/UX cannot change without user re-approval;
- legacy fences/polygon dogs/artifacts must be removed from runtime in the
  approved visual implementation, not hidden by new overlays;
- macOS-only current gate and exact Steam-managed Godot/process rules.

## Tooling state

Shelter MCP D-026 source-derived bundle is implemented and accepted. It remains
routine context routing when healthy; source docs are authority. Browser,
Mobile, new MCP and infrastructure work are frozen for the current shell wave.

## Next best step

First obtain independent docs verification of the direct user-approved exact
`15.0s` D-030-only readiness amendment. It is a spawn-anchored maximum;
successful readiness returns immediately. Route A still permits unique
system-temp observer/runner `HOME`/output only as transient source; complete
raw/evidence must be promoted into unique repo-local `tmp/` with deterministic
inventory, SHA-256 and byte/readback match before verifier handoff.

The independently passed named-profile config remains unchanged as background
configuration evidence and is not a production prerequisite. Permission/
private-tmp diagnosis is isolated into a separate non-blocking task. After docs
`PASS`, production-author task `019f8ee0-1cd9-76e0-846d-58184b4d945a`
continues under effective `:danger-full-access` / Full Access, with no
named-profile, Seatbelt, network-disabled, config reload or UI/profile action.
Pending bugfix bytes are:

```text
vertical_slice_demo.gd
  959660edfdace6a6325cc68c3240464e7099ba2c5eb0a97e13e51676cf02e974
test_d030_selected_h_current_presentation.py
  a1e17b2de75090bf66513c457e08b702d2988b18c8fa16495817593eca59e74c
```

The author may add only the smallest D-030 route-specific runner/observer/
profile wiring needed for exact `15.0s`. Shared/global timeout broadening or
other-profile changes are `STOP` before write. Static/protected/differential
gates precede exactly one governed run. Protected D-024, tooling, assets,
gameplay/save, roster and Checkpoint 2 remain unchanged/forbidden. Fresh
`12/12` / `24/24`, atomic matrix/readiness, relevant suites and Route-A
promotion remain required. First actual runner/process/validator/suite/
protected failure stops once with no retry. Tolerance, suppression, allowlist,
workload reduction and historical FAIL reinterpretation remain forbidden.
Separate independent mechanical `PASS` is required before Checkpoint 2 leaves
`NOT STARTED / HOLD`.
