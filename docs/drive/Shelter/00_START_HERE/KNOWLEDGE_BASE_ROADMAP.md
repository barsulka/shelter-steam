# KNOWLEDGE_BASE_ROADMAP — Shelter zero-wave documentation cleanup

Дата: 2026-07-21
Статус: active verification roadmap
Владелец: Project Manager / Producer
Назначение: зафиксировать нулевую волну Git-history cleanup и её независимую проверку.

Catalog status: active verification roadmap
Catalog current phase: Zero-wave authoring completed; independent verifier acceptance is pending.
Catalog next step: Independent verifier reviews authority edits line-by-line, retained exceptions, links, contradictions, counts and MCP source parsing; no author self-acceptance.

## 0. Purpose

Keep the zero-wave verification scope as the only active documentation roadmap.

## Current zero-wave scope

- canonical current memory synchronized to Visual Shell Lock → Interactive
  Shelter Shell;
- mutable obsolete/history Markdown deleted after inbound/dependency audit;
- documentation governance changed to Git-history retention;
- MCP-required fixed paths kept compact;
- D-024/hash/validator-bound bytes and Accepted ADR preserved;
- Art Director handoff prepared.

## Acceptance gate

Author sanity checks are evidence only. A separate verifier must review:

1. `PROJECTS_RULES.md` and `AGENTS.md` documentation-only diff line by line;
2. all current docs for one priority/next step/acceptance owner;
3. D-024 Contract A, whole-brief SHA and 2066-entry seal;
4. Labrador/D-024 pinned Markdown hashes;
5. MCP required-source parser/tests without code changes;
6. Markdown links and deleted-path references;
7. contradiction scan for old HOLD/UNSEALED/First 48 Hours active routes;
8. changed/deleted file scope and no runtime/code/assets/ADR mutation.

## Completion rule

After independent PASS, replace this document with a minimal maintenance
roadmap or update its phase. Do not preserve verifier logs as new history docs;
use Git/handback unless a validator requires exact evidence.
